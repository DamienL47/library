<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
{% block header %}
    <nav class="containerNav">
        <p> Bienvenue {{ app.user.email }}</p>
        <a class="btn-accueil" href="{{path('home')}}">Accueil</a>
        <a class="btn-accueil" href="{{path('admin_app_book_index')}}">Nos Livres</a>
        <a class="btn-accueil" href="{{path('app_admin_author_index')}}">Les Auteurs</a>
        <a class="btn-accueil" href="{{path('app_logout')}}">Déconnexion</a>
        <form action="{{ path('search_bar') }}" method="get" class="formulaire">
            <label for="search" class="form-label"></label>
            <input class="form-control" type="text" name="search" id="search" placeholder="Chercher sur le site " >
            <input class="btn btn-light" type="submit">
        </form>
    </nav>
{% endblock %}
{% block body %}{% endblock %}
</body>
<footer>
    {% block footer%}{% endblock %}
</footer>
</html>