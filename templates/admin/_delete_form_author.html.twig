<form method="post" action="{{ path('app_admin_author_delete', {'id': author.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet Auteur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button class="btn">Supprimer</button>
</form>
